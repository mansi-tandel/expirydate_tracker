# MongoDB Atlas Connection (Primary)
MONGODB_URI=

JWT_SECRET=
PORT=

# SMTP (configure these)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Cron timezone (set to your local TZ, e.g., Asia/Kolkata)
CRON_TZ=

# For testing: run the daily job once on server start (true/false)
RUN_CRON_IMMEDIATELY=
